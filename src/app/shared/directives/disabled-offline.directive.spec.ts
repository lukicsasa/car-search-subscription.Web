import { DisabledOfflineDirective } from './disabled-offline.directive';

describe('DisabledOfflineDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledOfflineDirective();
    expect(directive).toBeTruthy();
  });
});
